# API Folder README — {domain_name}

---

## Purpose

This folder documents the **API surface** for `{domain_name}`.

- One file per endpoint
- Consistent structure (see `docs/project/templates/api-endpoint.md`)
- Diagrams use Mermaid `sequenceDiagram`

---

## Base path

- `/api/v1/{domain-base-path}`

---

## Endpoints

| Method | Path | Doc |
|--------|------|-----|
| POST | `/example` | `example.md` |